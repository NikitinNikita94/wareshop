server:
  servlet:
    context-path: /app
  port: ${SERVER_PORT:8080}

spring:
  profiles:
    active: dev

app:
  pathToFile: src/main/resources/static/out/products.txt
  scheduling:
    enabled: false
    period: 60000 #ms
    priceIncreasePercentage: 10.7
    optimization: false
  rest:
    currency-service:
      host: http://localhost:8082 # host второго сервиса
      methods:
        - /api/v2/currencies # url эндпоинта с курсами для GET запроса
        - /api/v2/account-number # url эндпоинта для POST запроса для получения account-number
        - /api/v2/account-inn  # url эндпоинта для POST запроса для получения account-inn
      mock:
        enabled: false
      file: src/main/resources/json/exchange-rate.json

logging:
  level:
    root: info