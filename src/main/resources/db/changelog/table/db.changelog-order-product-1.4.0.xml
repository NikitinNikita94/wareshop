<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.9.xsd">

    <changeSet id="1" author="Nikitin-Nikita">
        <preConditions>
            <not>
                <tableExists tableName="order_product"/>
            </not>
        </preConditions>
        <createTable tableName="order_product">
            <column name="order_id" type="UUID">
                <constraints nullable="false" foreignKeyName="fk_order_product" references="orders(id)"/>
            </column>
            <column name="product_id" type="UUID">
                <constraints nullable="false" foreignKeyName="fk_product_order" references="product(id)"/>
            </column>
            <column name="quantity" type="INT">
                <constraints nullable="false"/>
            </column>
            <column name="price" type="decimal">
                <constraints nullable="false"/>
            </column>
        </createTable>
        <addPrimaryKey tableName="order_product" columnNames="order_id,product_id"
                       constraintName="primary_key_constraint"/>
        <rollback>
            <dropTable tableName="order_product"/>
        </rollback>
    </changeSet>
</databaseChangeLog>
